# Free Fall Scenario

This scenario project assembles the free-fall example packages into a runnable setup.

The manifest lives in `scenario.duan`. Run outputs should be written under `runs/`, and tool-managed package, schema, and runner caches should live under `.duan/`.
