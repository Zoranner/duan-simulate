# duan-runner-generator

Generated runner project writer.

This package will generate a thin Rust runner crate with registry dependencies, `.cargo/config.toml`, and `main.rs` that installs selected DUAN packages and runs a scenario.
